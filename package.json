{
  "name": "school-paper-request-application",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node scripts/start.mjs",
    "build": "dotnet build Backend/Backend.csproj && npm --prefix frontend run build",
    "test": "dotnet test Backend.Tests/Backend.Tests.csproj && npm --prefix frontend run lint"
  },
  "dependencies": {
    "google-auth-library": "^11.0.0",
    "nodemailer": "^9.0.5"
  }
}
